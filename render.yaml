# Render Static Site configuration
# Plain HTML/CSS/JavaScript — no build step. index.html is the entry page.
# Deployed directly from the repository root.
services:
  - type: web
    name: sales-data-training-academy
    runtime: static
    buildCommand: ""
    staticPublishPath: .
